---
import "./Card.scss";

const { title, image, language } = Astro.props;
---

<div class="card">
  {
    image && (
      <img
        class="card-image"
        src={image}
        width="auto"
        height="72px"
        alt="Main image"
      />
    )
  }
  <div class="card-info">
    {title && <h1 class="card-title">{title}</h1>}
    {
      language && (
        <h4 class="card-language">{language === "es" ? "🇪🇸" : "🇬🇧"}</h4>
      )
    }
  </div>
</div>
