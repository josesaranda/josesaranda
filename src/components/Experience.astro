---
import { t } from "i18next";
const experiences = [
  "fortris",
  "shapelets",
  "sombradoble",
  "nowait",
  "sombradoble-intern",
];
---

<h3><span class="emoji">🔬</span> {t("experience.title")}</h3>
<p>
  {t("experience.paragraph1")}
</p>
<section class="experience">
  {
    experiences.map((experience) => {
      return (
        <div>
          <h4 set:html={t(`experience.experiences.${experience}.title`)} />
          <h5 class="dates">
            {t(`experience.experiences.${experience}.dates`)}
          </h5>
          <p>{t(`experience.experiences.${experience}.description`)}</p>
        </div>
      );
    })
  }
</section>
<style>
  .experience div {
    padding: 10px;
    margin-top: 16px;
    margin-bottom: 16px;
  }
  .experience div:not(:last-child) {
    border-bottom: 1px solid var(--c-primary);
  }
</style>
